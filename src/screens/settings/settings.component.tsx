import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const Settings: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Settings</Text>
    </SafeAreaView>
  );
};
